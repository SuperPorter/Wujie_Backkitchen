<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.txd.hzj.wujie_backkitchen"
    >

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <meta-data
            android:name="design_width_in_dp"
            android:value="1280"/>
        <meta-data
            android:name="design_height_in_dp"
            android:value="1920"/>

        <activity android:name=".UI.Activity.Back_kitchen_Login"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.Activity.HomeActivity"
            android:screenOrientation="landscape"/>
        <activity android:name=".UI.Activity.OrderdetailsActivity" />
        <activity android:name=".UI.Activity.Food_detailsActivity" />
        <activity android:name=".UI.Activity.IngredientsreplacementActivtiy" />
    </application>

</manifest>